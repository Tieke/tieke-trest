
<%= erb :navbar %>

<h1>Recent posts:</h1>

<% if @posts[0].size > 0 %>
	<% @posts[0].each_with_index do |post, post_index| %>
		<img src="<%= post.img_url %>">
		<p><%= post.caption %></p>
	<% end %>
<% end %>
